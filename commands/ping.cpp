#include"../include/the_include.hpp"

// void	ping_cmd(std::istringstream *iss, std::string word, Client *user)
// {
// 	if (*iss >> word) {
// 		std::string str = PONG(user);
// 		send(user->fd, str.c_str(), str.length(), MSG_NOSIGNAL);
// 		std::cerr << str << std::endl;
// 	} else {
// 		std::string msg = ERR_NEEDMOREPARAMS(user, "PING");
// 		send(user->fd, msg.c_str(), msg.length(), MSG_NOSIGNAL);
// 	}
// }

